//WHILE
var num = 30; //Definimos una variable de tipo numerico y le asignamos un valor de 
while (num > 0) {         //iniciamos el bucle con la variable en ceros
    if (num % 3 == 0) { //agregamos la condicion 
        console.log(num); //mostramos por consola 
    }
    num-- //contador disminuendo 
}
